return {
  "nvim-tree/nvim-tree.lua",
  opts = {
    filters = {
      dotfiles = false,     -- Shows .env files
      git_ignored = false,  -- Shows files even if they are in your .gitignore
    },
  },
}
